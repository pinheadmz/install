use git
not docker (but spin up a docker container to expiriment as a vps)
node alreayd

#-2 display hats instlaled / running
# -1 : start stop or isntall or reconfigure?
  PID locks

0. where do you wanna install (node: os.homedir) PREFIX!
1. library: (bcoin bcash hsd)
  hard code bcash ports, no matter what
2. network: (main test regtest simnet)
  simnet: peers or seeds
  hsd: no main yet
3. node: full spv prune neutrino
4. wallet? --nowallet
 bmultisig (--nowallet)
5. bpanel? (from git, plugins form npm)
  6. plugins?
    easy: genesis / bwallet / connection manager / price widget / recent blocks
    adv: select from list (npm search or hard code)



installing:
  genreate api keys bcrypto 32bytes


post install
  ask if user is ready
    reveal api key
    reveal primary wallet mnemonic
  clear screen
  open default browser :5000
  have fun, come back here to stop


script closes